import { Routes } from '@angular/router';
import { DashboardConductor } from "../dashboard-conductor.component";

export const CONDUCTOR_ROUTES: Routes = [
    {
        path: '',
        component: DashboardConductor
    }
];